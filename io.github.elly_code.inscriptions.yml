id: io.github.elly_code.inscriptions
# elementary SDK is not available on Flathub, so use the elementary BaseApp instead
base: io.elementary.BaseApp
base-version: circe-25.08
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: io.github.elly_code.inscriptions

finish-args:
  - --share=ipc
  - --device=dri
  - --socket=fallback-x11
  - --socket=wayland
  # Required for communication with external API
  - --share=network

modules:
  - name: inscriptions
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/elly-code/inscriptions.git
        tag: 1.1.0
        commit: c159de9987366f09ba943026ad162d768b1903a9
        x-checker-data:
          type: git
          tag-pattern: '^([\d.]+)$'
